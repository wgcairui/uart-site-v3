
---

## Docker build cache hit — git reset --hard 后必须 --no-cache (2026-06-28)

**踩坑**: hotfix commit 后 SSH prod + `git reset --hard origin/main` + `docker build -t xxx:latest .`。build log 显示**所有 stage 全 CACHED**（包括 `#8 COPY . .` 和 `#12 RUN bun run build`），image hash 跟之前一样。但 prod working tree 已经更新到 hotfix 代码。

**根因**: Docker build cache key = build context 文件 hash。`git reset --hard` 改了 working tree 但 docker daemon 可能用了陈旧 snapshot / 内核 page cache 没刷，hash 算出旧值 → cache hit。

**症状**:
- `git log --oneline` 在 prod 显示新 commit
- `git diff old..new` 显示文件确实改了
- `docker build` log 所有 stage CACHED
- image hash 跟之前一样（**没真重新 build**）
- compose up 用旧 image，hotfix 没生效

**修法**: 强制 `--no-cache` rebuild:

```bash
ssh prod "HTTPS_PROXY=... nohup docker build --no-cache -t xxx:latest . > /tmp/build.log 2>&1 &"
# 注意：--no-cache 全重新 build（5+ min 长跑，按 § nohup 模式）
```

**预防 checklist（hotfix deploy 后必跑）**:

```bash
# build 完先 verify image hash 跟之前不一样
docker images xxx:latest --format '{{.ID}}'
# 跟之前部署的 hash 对比（之前 log #16 writing image sha256:xxx）
# 如果 hash 一样 → 立刻 --no-cache 重 build
```

WHY: 2026-06-28 oss hotfix deploy，第一次 docker build cache hit 没察觉，浪费一次 build + compose up 时间。第二次 `--no-cache` 强制 rebuild 才正确（image hash b81d834 跟之前 ed557d 不同）。
